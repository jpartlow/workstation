# VMPOOLER-BEGIN
# Example (replace ip address, sudo `exportfs -a` locally and `mount
# [workstation ip]:/home/jpartlow/work/src /local-src` on the test host after
# creating the directory):
#"/home/<%= @account %>/work/src" 10.32.115.68(ro,no_subtree_check,all_squash,anonuid=1000,anongid=1000,fsid=3725123654)
# VMPOOLER-END
